'use client';
export function MatchesTab({ tournamentId, revision }: { tournamentId: string; revision: number }) {
  void tournamentId;
  void revision;
  return <div className="py-6 text-muted-foreground">賽程 tab 即將推出</div>;
}

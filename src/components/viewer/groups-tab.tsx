'use client';
export function GroupsTab({ tournamentId, revision }: { tournamentId: string; revision: number }) {
  void tournamentId;
  void revision;
  return <div className="py-6 text-muted-foreground">分組 tab 即將推出</div>;
}

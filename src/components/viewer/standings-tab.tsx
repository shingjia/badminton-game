'use client';
export function StandingsTab({ tournamentId, revision }: { tournamentId: string; revision: number }) {
  void tournamentId;
  void revision;
  return <div className="py-6 text-muted-foreground">排名 tab 即將推出</div>;
}
